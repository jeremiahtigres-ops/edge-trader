[build]
  command = "npm run build"
  publish = ".next"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."bot-cycle-background"]
  timeout = 900

[[redirects]]
  from = "/api/bot/*"
  to = "/.netlify/functions/:splat"
  status = 200